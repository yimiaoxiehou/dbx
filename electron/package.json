{
  "name": "dbx-electron",
  "version": "0.1.0",
  "private": true,
  "main": "main.js",
  "description": "Electron shell for dbx (loads dbx-web over HTTP).",
  "scripts": {
    "start": "electron ."
  }
}
